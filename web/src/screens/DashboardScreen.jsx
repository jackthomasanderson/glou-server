import React from 'react';
import ProDashboard from '../components/ProDashboard';

/**
 * DashboardScreen - Main professional dashboard
 * 
 * Displays:
 * - KPI cards (total bottles, ready to drink, etc.)
 * - Type distribution pie chart
 * - Cave distribution bar chart
 * - Apogee timeline
 * - Recent bottles
 */
const DashboardScreen = () => {
  return <ProDashboard />;
};

export default DashboardScreen;
