services:
  db:
    image: postgres:16-alpine
    container_name: glou-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-glou}
      POSTGRES_USER: ${POSTGRES_USER:-glou}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-glou}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
